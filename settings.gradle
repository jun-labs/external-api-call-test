pluginManagement {
    repositories {
        gradlePluginPortal()
        mavenCentral()
    }
    plugins {
        id "org.springframework.boot" version "3.0.10"
        id "io.spring.dependency-management" version "1.1.3"
    }
}

rootProject.name = "callcount"
